<?php

$_maths = 50;
$_english = 78;
$_= 90;

echo "Total marks:".($_maths+$_english+$_);

// in php, string are joined {concat} with period symbol (.) 
// but integers are added with (+) symbols.
// the expression wrapped in bracket are processed first then after the code is executed